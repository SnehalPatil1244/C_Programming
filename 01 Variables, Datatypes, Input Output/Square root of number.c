#include<stdio.h>
#include<conio.h>

int main()
{

    int squareroot =0, No=0;

    printf("Enter the Number :");
    scanf("%d",&No);

    (squareroot = sqrt(No));

    printf("the square root of %d :%d",No,squareroot);

    getch();
    return 0;
}
